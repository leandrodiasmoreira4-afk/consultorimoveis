import type { PropertyWithRelations } from '../lib/supabase/types';

/**
 * Development fallback only.
 * Keep this empty until real, approved property data is supplied.
 */
export const mockProperties: PropertyWithRelations[] = [];
