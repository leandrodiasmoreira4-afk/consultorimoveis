export default function AdminFoundationPage() {
  return (
    <section className="page-hero" style={{ minHeight: "70svh" }}>
      <div className="container-wide">
        <div className="eyebrow">Área administrativa</div>
        <h1>Admin</h1>
        <p className="muted" style={{ maxWidth: 620, marginTop: 40 }}>Estrutura reservada para evolução futura. O painel administrativo completo ainda não foi implementado.</p>
      </div>
    </section>
  );
}
